---
const {
  ele = "h1",
  hlstyle = "hl-one",
  classes,
  modifier,
  ...rest
} = Astro.props;
const Tag = ele;
---

<Tag {...rest} class:list={[`hl-${hlstyle}`, classes, modifier]}>
  <slot />
</Tag>
