---
const { data, reviews } = Astro.props;
import ReviewCard from "./ReviewCard.astro";
---

<div class="reviews">
  <div class="reviews-wrapper wrap-med">
    {
      reviews.map((review) => {
        return <ReviewCard review={review} />;
      })
    }
  </div>
</div>
